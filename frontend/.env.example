# Since the ".env" file is gitignored, you can use the ".env.example" file to
# build a new ".env" file when you clone the repo. Keep this file up-to-date
# when you add new variables to `.env`.

# This file will be committed to version control, so make sure not to have any
# secrets in it. If you are cloning this repo, create a copy of this file named
# ".env" and populate it with your secrets.

# When adding additional environment variables, the schema in "/src/env.js"
# should be updated accordingly.

# Prisma
# https://www.prisma.io/docs/reference/database-reference/connection-urls#env
DATABASE_URL="postgresql://postgres:password@localhost:5432/frontend"

BETTER_AUTH_SECRET="your-secret-here"
BETTER_AUTH_URL="http://localhost:3000"

POLAR_ACCESS_TOKEN="your-access-token"
POLAR_WEBHOOK_SECRET="your-access-secret"

AWS_ACCESS_KEY_ID="your-aws-key"
AWS_SECRET_ACCESS_KEY="your-aws-secret"
AWS_REGION="your-aws-region"
AWS_S3_BUCKET_NAME="your-s3-bucket"
AWS_S3_BUCKET_URL="https://your-bucket-name.s3.your-region.amazonaws.com"

MODAL_API_URL="your-modal-endpoint"
MODAL_API_KEY="your-modal-key"
MODAL_API_SECRET="your-modal-secret"

# Backend (Modal)
MODAL_APP_NAME="aurasync-backend"
MODAL_S3_SECRET_NAME="aurasync-aws-secret"
MODAL_S3_BUCKET_NAME="aurasync-storage"
MODAL_HF_CACHE_VOLUME="aurasync-hf-cache"